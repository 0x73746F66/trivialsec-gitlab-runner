concurrent = 1
check_interval = 0

[session_server]
  session_timeout = 1800

[[runners]]
  name = "trivialsec-shared"
  url = "https://gitlab.com/"
  token = "TOKEN"
  executor = "shell"
  cache_dir = "/cache"
  [runners.cache]
    Insecure = false
